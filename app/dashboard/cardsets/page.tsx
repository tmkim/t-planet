import { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Invoices'
}

export default async function Page(){
    return (
        <div className="w-full">
            Card Set Page Placeholder
        </div>
    )
}